/*
	tcp listen的时候backlog参数的含义

		listen(sockfd, backlog)


		在TCP编程中，listen 函数用于将套接字转换为监听套接字，以便接受来自客户端的连接请求。
		listen 函数的一个重要参数是 backlog，它指定了内核应该为该套接字排队的最大连接请求数量。
		具体来说，backlog 参数的含义如下：

		排队的连接请求
			未完成连接队列：这是存储尚未完成三次握手的连接请求的队列。
				这些连接请求已经到达服务器，但还没有完成三次握手。

			已完成连接队列：这是存储已经完成三次握手的连接请求的队列。
				这些连接请求已经准备好被 accept 函数处理。

		backlog 参数的作用
			最大排队长度：backlog 参数指定了这两个队列的总长度。
				当新的连接请求到达时，如果队列已满，新的连接请求将被拒绝。

			系统限制：实际的排队长度可能会受到操作系统的限制。
				在某些系统中，backlog 参数会被调整为一个合理的值，以防止队列过长导致性能问题。

		具体行为
			未完成连接队列：当客户端发送SYN报文段时，服务器将其放入未完成连接队列，
				并发送SYN-ACK报文段。如果队列已满，新的连接请求将被丢弃。

			已完成连接队列：当客户端发送ACK报文段完成三次握手后，
				连接请求从未完成连接队列移到已完成连接队列。
				accept 函数从已完成连接队列中取出连接请求并创建新的套接字。

		选择合适的 backlog 值
			低负载情况：如果服务器的负载较低，可以选择较小的 backlog 值，例如5或10。
			高负载情况：如果服务器的负载较高，需要处理大量的并发连接请求，可以选择较大的 backlog 值，例如128或256。
			系统默认值：许多操作系统提供了默认的 backlog 值，通常为50或128。可以根据实际情况调整这个值。


		backlog 参数的实际意义
			未完成连接队列：存储尚未完成三次握手的连接请求。
			已完成连接队列：存储已经完成三次握手但尚未被 accept 函数处理的连接请求。
			总长度：backlog 参数指定了这两个队列的总长度。
*/